section#program.my-4
  h2.mb-3 プログラム
  p 敬称略
  
  each timeslot, idx in program
    div.row.my-3.slot(data-date=workshop.datejs, data-start=timeslot.start, data-end=timeslot.end)
      div.col-sm-2.col-md-2 #{timeslot.start} ~ #{timeslot.end}
      div.col-sm-10.col-md-5.bold #{timeslot.title}
      div.col-sm-12.col-md-5.text-right #{timeslot.owner}
    case timeslot.type
      when 'nonsession'
        unless idx == program.length - 1
          hr
      when 'session'
        div.card.my-3.bg-transparent
          div.card-body.bg-transparent
            each presentation in timeslot.session
              div.row.my-2.slot(data-date=workshop.datejs, data-start=presentation.start, data-end=presentation.end)
                div.col-sm-2.col-xl-2 #{presentation.start} ~ #{presentation.end}
                div.col-sm-10.col-xl-7 #[mark.blue #{presentation.title}]
                div.col-sm-12.col-xl-3.text-right #{presentation.presenter}

  p.text-right
    a#print_program(role="button" href="#") プログラムを印刷する
